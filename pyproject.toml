[project]
name = "kev_check"
version = "0.2.2"
description = "Check for updates to CISA Known Exploited Vulnerabilities list"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "colorama>=0.4.6",
    "openpyxl>=3.1.5",
    "pyyaml>=6.0.3",
    "requests>=2.32.5",
]
